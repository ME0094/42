# Projects

Create one subdirectory per permitted 42 School project.

Before committing:

- confirm that the material may be stored in a private repository;
- remove credentials, personal data, generated binaries, and evaluation artifacts;
- document how to build and test the project;
- keep UoPeople coursework in `ME0094/uopeople-cs`.
